version: 1.0
runtime: python3
build:
  commands:
    build:
      - yum install -y cargo
      - pip install -r requirements.txt
run:
  command: python main.py
  network: 
    port: 8080
    env: PORT
  env:
    - name: AWS_REGION
      value: us-west-2
